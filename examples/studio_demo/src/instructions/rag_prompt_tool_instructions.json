[
    {
      "type": "function",
      "function": {
        "name": "chroma_query",
        "description": "Performs a RAG query using a Chroma Vector Database.",
        "parameters": {
          "type": "object",
          "properties": {
            "prompt": {
              "type": "string",
              "description": "The prompt to respond to."
            },
              "template": {
                "type": "string",
                "default": "chroma_query.md",
                "description": "The name of the prompt template file to use."
              }
          },
          "required": [
            "query"
          ]
        }
      }
    }
]